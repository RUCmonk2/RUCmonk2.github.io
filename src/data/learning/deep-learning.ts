import type { CourseChapter } from "./types";

export const deepLearningChapters = [
  {
    id: "foundations",
    title: "先读懂一个学习问题",
    group: "一 · 建立语言",
    lead: "把数据、模型、损失和参数分清楚，后面的公式才有落点。先用一个两特征的预测问题把它们连起来。",
    source: "自学讲义 v1.1，第 1 章；课程第一、二周的符号与模型部分。",
    body: "## 从输入到预测\n\n监督学习的数据由输入与标签组成：\n\n$$\n\\mathcal D=\\{(\\mathbf x_i,y_i)\\}_{i=1}^{N},\\qquad \\mathbf x_i\\in\\mathbb R^d.\n$$\n\n$N$ 表示样本数，$d$ 表示每个样本的特征数。模型 $f_\\theta(\\mathbf x)$ 接收输入；训练调整的是参数 $\\theta$。预测新样本时，通常固定参数，只计算前向结果。\n\n以一个简化的房价模型为例，输入是经过尺度处理的面积与房龄，模型是：\n\n$$\n\\hat y=\\mathbf w^\\top\\mathbf x+b.\n$$\n\n取 $\\mathbf x=(2,1)^\\top$、$\\mathbf w=(3,-1)^\\top$、$b=4$，得到 $\\hat y=3\\times2-1\\times1+4=9$。这里的权重描述模型中的关系，本身不能证明因果关系。\n\n## 先检查维度，再检查数值\n\n本笔记默认单样本是列向量，批量数据则每行存一个样本。\n\n| 对象 | 形状 | 含义 |\n| --- | --- | --- |\n| $\\mathbf x$ | $d\\times1$ | 一个样本 |\n| $X$ | $N\\times d$ | 一批样本 |\n| $\\mathbf w$ | $d\\times1$ | 特征权重 |\n| $X\\mathbf w+b\\mathbf1$ | $N\\times1$ | 一批预测 |\n\n内积 $\\mathbf w^\\top\\mathbf x$ 产生标量；外积 $\\mathbf x\\mathbf y^\\top$ 产生矩阵；逐元素乘积 $\\mathbf x\\odot\\mathbf y$ 保持原来的形状。反向传播中三者都会出现，不能互换。\n\n## 梯度是怎样来的\n\n取平方损失 $J(w)=\\frac12(wx-y)^2$。先给中间误差起名字 $e=wx-y$，再应用链式法则：\n\n$$\n\\frac{\\partial J}{\\partial w}\n=\\frac{\\partial J}{\\partial e}\\frac{\\partial e}{\\partial w}\n=e\\,x=(wx-y)x.\n$$\n\n梯度把所有参数的偏导数排在一起，指出目标在当前参数附近增大最快的方向。若一个参数经多条路径影响损失，各条路径的贡献要相加。例如 $J=w^2+3w$ 的导数是 $2w+3$。\n\n> 每遇到一个新公式，先问：哪个量是输入，哪个量在训练时改变，结果应该是什么形状？",
    checks: [
      {
        question: "X 是 32×237，W 是 237×3，XW 的形状是什么？",
        answer: "结果是 **32×3**：32 个样本，每个样本对应 3 个输出分数。",
      },
      {
        question: "J(w)=½(2w−3)²，在 w=1 处的导数是多少？",
        answer:
          "$J'(w)=(2w-3)\\times2$，代入 $w=1$ 得 **$-2$**。负号表示在这里稍微增加 $w$ 会降低损失。",
      },
    ],
  },
  {
    id: "representations",
    title: "模型如何学到表示",
    group: "一 · 建立语言",
    lead: "模型不仅可以学习怎样分类，也可以学习用什么特征描述输入。这是理解深度学习的一条主线。",
    source: "自学讲义 v1.1，第 2 章；第一周的表示学习部分。",
    body: "## 表示决定了后面的任务有多难\n\n假设你要把图片分为猫与狗。原始像素描述亮度和颜色，而任务需要形状、部件和对象等信息。表示学习在两者之间学习一个变换：\n\n$$\n\\hat y=g(\\phi(\\mathbf x)).\n$$\n\n$\\phi$ 把原始输入转成表示，$g$ 利用表示完成任务。深度网络把多层变换组合起来；各层可以逐步组织有用的信息，但不能默认每个神经元都恰好对应一个能命名的概念。\n\n## One-hot 与嵌入的区别\n\n用 one-hot 表示三个对象，可以得到 $(1,0,0)$、$(0,1,0)$、$(0,0,1)$。这种编码很清楚地表达“它们不同”，但任意两个不同对象的内积都是零，没有告诉我们谁和谁更相似。\n\n嵌入表 $E\\in\\mathbb R^{V\\times k}$ 为 $V$ 个对象分别存储一个 $k$ 维向量。若 $\\mathbf x$ 是 one-hot 列向量，$E^\\top\\mathbf x$ 就取出对应向量。\n\n| 表示 | 能直接表达什么 | 需要注意什么 |\n| --- | --- | --- |\n| One-hot | 类别身份与相异性 | 维度随类别数增长 |\n| 学习到的嵌入 | 共享坐标中的关系 | 关系依赖数据与训练目标 |\n| 随机稠密向量 | 一种数值编码 | 稠密不等于有语义 |\n\n把坐标画成二维散点图只是查看表示的一种方式；降维本身可能改变距离，图上靠近不能单独证明语义相似。\n\n## 端到端训练怎样连接两部分\n\n如果先固定人工特征，再训练分类器，分类误差一般不会改变特征提取规则。端到端训练则让最终任务的损失影响各个可学习模块：\n\n$$\n\\mathbf x\\longrightarrow\\phi_1\\longrightarrow\\phi_2\n\\longrightarrow g\\longrightarrow\\ell.\n$$\n\n反向传播把损失对参数的影响传回去，因此“该提取什么特征”也成为学习的一部分。端到端并不意味着没有数据清洗、预处理或模块划分。\n\n## 什么时候从简单模型开始\n\n特征已经能很好表达任务、样本数量有限时，线性模型是有价值的基线。原始图像或音频等感知数据，往往更需要学习表示。模型是否合适，要在合理的数据划分与评价指标下判断，不能只看结构是否复杂。",
    checks: [
      {
        question: "把 one-hot 换成随机生成的低维向量，就学到了语义吗？",
        answer:
          "没有。低维、稠密只是表示形式。有用的关系通常来自合适的训练数据、目标和优化过程。",
      },
    ],
  },
  {
    id: "generalization",
    title: "训练得好，为什么还会预测错",
    group: "二 · 学会训练",
    lead: "训练集是模型已经见过的题目。我们关心的是它遇到新题时的表现，经验风险与泛化描述的正是这层差别。",
    source: "自学讲义 v1.1，第 3 章；第一、二周的风险与泛化部分。",
    body: "## 真正想优化的量与能算出的量\n\n若真实数据分布为 $P$，我们关心模型在该分布下的平均损失：\n\n$$\nR(f)=\\mathbb E_{(\\mathbf x,y)\\sim P}[\\ell(y,f(\\mathbf x))].\n$$\n\n这个**期望风险**通常无法直接算出。手里只有有限数据时，计算的是**经验风险**：\n\n$$\n\\widehat R_{\\mathcal D}(f)=\\frac1N\\sum_{i=1}^N\\ell(y_i,f(\\mathbf x_i)).\n$$\n\n训练从候选函数中挑选经验风险较小的模型。关键在于：挑选过程使用了这批数据，模型可能同时利用了规律与样本中的偶然噪声。\n\n> 对固定函数，样本平均可以估计总体平均；训练出来的函数却依赖样本。前一个结论不能直接保证训练后的模型不会过拟合。\n\n## 用三份数据回答三个问题\n\n| 数据 | 回答的问题 | 用途 |\n| --- | --- | --- |\n| 训练集 | 参数怎样调整？ | 计算损失和梯度 |\n| 验证集 | 哪种配置更合适？ | 选择结构、学习率与停止时刻 |\n| 测试集 | 最终方案表现如何？ | 开发完成后评价 |\n\n标准化的均值和方差也需要从训练集估计，再应用到验证、测试数据。反复依据测试结果改模型，会使测试集参与开发，失去独立评价的作用。\n\n## 从现象到诊断\n\n训练和验证都差，先检查数据、模型表达和优化是否正常；训练很好而验证差，检查过拟合、数据泄漏和分布差异；损失剧烈波动，检查学习率、输入尺度与数值有限性。这些都是线索，不是仅凭一条曲线就能确定的原因。\n\n## 正则化改变了训练偏好\n\n常用的 $L_2$ 正则目标为：\n\n$$\nJ(\\theta)=\\widehat R_{\\mathcal D}(f_\\theta)+\\frac{\\lambda}{2}\\|\\mathbf w\\|_2^2.\n$$\n\n第二项惩罚过大的权重，其梯度为 $\\lambda\\mathbf w$。这里保留了 $\\frac12$；如果目标写成 $\\lambda\\|\\mathbf w\\|^2$，梯度就要写成 $2\\lambda\\mathbf w$。\n\n正则化、数据增强和提前停止从不同方面约束拟合过程。它们是否改善新数据表现，仍要通过验证来判断。",
    checks: [
      {
        question:
          "训练误差从 3.6 降到 3.1，验证误差从 3.9 升到 4.1，应直接选择后一个模型吗？",
        answer:
          "不能。若两次评价在同一验证集、同一指标下可比，前一个模型的验证表现更好。训练误差下降不自动代表泛化改善。",
      },
      {
        question: "为什么不能每轮看测试集，然后据此调整学习率？",
        answer:
          "这样测试集就参与了模型选择。最终测试结果会受到开发过程对这批数据的适应，不能再当作独立评价。",
      },
    ],
  },
  {
    id: "losses",
    title: "损失函数在惩罚什么",
    group: "二 · 学会训练",
    lead: "损失把“预测得怎样”变成一个可优化的数。回归常比较残差，分类常比较给正确类别分配了多少概率。",
    source: "自学讲义 v1.1，第 4 章；第一、二周的损失函数部分。",
    body: "## 回归：同一残差的三种度量\n\n令 $e_i=\\hat y_i-y_i$，则\n\n$$\n\\mathrm{MSE}=\\frac1N\\sum_i e_i^2,\\qquad\n\\mathrm{RMSE}=\\sqrt{\\frac1N\\sum_i e_i^2},\\qquad\n\\mathrm{MAE}=\\frac1N\\sum_i|e_i|.\n$$\n\n若两次预测的残差是 $1$ 和 $3$，MSE 为 $5$，RMSE 为 $\\sqrt5\\approx2.236$，MAE 为 $2$。平方更强地放大了大残差；RMSE 与标签同单位，MSE 的单位则是标签单位的平方。\n\n没有正则项时，MSE 与 RMSE 因平方根单调而有相同的最优解，但它们的梯度并不一样。\n\n## 线性回归的梯度\n\n对 $\\mathbf e=X\\mathbf w+b\\mathbf1-\\mathbf y$，选择\n\n$$\nJ=\\frac{1}{2N}\\mathbf e^\\top\\mathbf e+\\frac{\\lambda}{2}\\|\\mathbf w\\|^2,\n$$\n\n得到\n\n$$\n\\nabla_{\\mathbf w}J=\\frac1N X^\\top\\mathbf e+\\lambda\\mathbf w,\\qquad\n\\frac{\\partial J}{\\partial b}=\\frac1N\\mathbf1^\\top\\mathbf e.\n$$\n\n若数据项改成 RMSE，记 $r=\\sqrt{\\mathbf e^\\top\\mathbf e/N}>0$，数据梯度还要除以 $r$：\n\n$$\n\\nabla_{\\mathbf w}J=\\frac{X^\\top\\mathbf e}{Nr}+\\lambda\\mathbf w.\n$$\n\n不能在说明里写 RMSE，却在计算时无说明地使用 MSE 的梯度。零残差处也不能直接除以零。\n\n## 分类：正确类别的概率\n\n三分类预测为 $\\mathbf p=(0.1,0.7,0.2)$，真实类别是第二类。one-hot 标签为 $\\mathbf y=(0,1,0)$，交叉熵为\n\n$$\n\\ell=-\\sum_c y_c\\log p_c=-\\log0.7\\approx0.3567.\n$$\n\n只需要正确类别那一项，不是把三个负对数求平均。正确类别概率越接近 1，损失越接近 0。这里用自然对数。\n\n## 交叉熵与 KL 的联系\n\n$$\nD_{\\mathrm{KL}}(\\mathbf y\\|\\mathbf p)=H(\\mathbf y,\\mathbf p)-H(\\mathbf y).\n$$\n\n当标签分布固定时，$H(\\mathbf y)$ 是常数，因此最小化交叉熵与最小化该 KL 散度等价。KL 一般不对称，不能把它当成通常意义的距离。\n\n概率很小时直接取对数容易出现数值问题。后面 Softmax 一节 的稳定实现会直接从分数计算损失。",
    checks: [
      {
        question: "预测类别没有改变，交叉熵还会改变吗？",
        answer:
          "会。例如正确类别的概率从 $0.4$ 变为 $0.8$，两次都可能预测正确，但损失由 $-\\log0.4\\approx0.9163$ 降到 $-\\log0.8\\approx0.2231$。",
      },
    ],
  },
  {
    id: "optimization",
    title: "亲手做一次梯度下降",
    group: "二 · 学会训练",
    lead: "梯度告诉我们当前往哪里走，学习率决定这一步走多远。一个只有两个参数的模型，已经能展示训练的完整循环。",
    source: "自学讲义 v1.1，第 5 章；第一、二周的梯度下降部分。",
    lab: "gradient",
    body: "## 为什么沿负梯度更新\n\n梯度下降使用当前参数处的梯度：\n\n$$\n\\theta_{t+1}=\\theta_t-\\alpha\\nabla J(\\theta_t).\n$$\n\n一阶近似中，沿方向 $\\mathbf d$ 移动一小步的损失变化约为 $\\alpha\\nabla J^\\top\\mathbf d$。取 $\\mathbf d=-\\nabla J$，变化约为 $-\\alpha\\|\\nabla J\\|^2$。这个结论依赖步长足够小，不能理解成任意学习率都会下降。\n\n## 例题：一次更新算到底\n\n数据是 $x=2,y=3$，模型是 $\\hat y=wx+b$，损失是 $J=\\frac12(\\hat y-y)^2$。从 $w=b=0$ 开始。\n\n| 计算 | 结果 |\n| --- | --- |\n| 预测 $\\hat y$ | $0$ |\n| 残差 $e=\\hat y-y$ | $-3$ |\n| 损失 $J$ | $4.5$ |\n| 权重梯度 $ex$ | $-6$ |\n| 偏置梯度 $e$ | $-3$ |\n\n学习率 $\\alpha=0.1$ 时，$w'=0.6,b'=0.3$，预测变为 $1.5$，新损失是 $1.125$。若改成 $\\alpha=1$，预测变为 $15$，损失上升到 $72$。\n\n> 所有梯度都在同一组旧参数上计算。算齐之后，再统一更新参数。\n\n## 一次更新看多少数据\n\n全量梯度下降每次使用全部训练样本；单样本 SGD 每次使用一个样本；小批量训练每次使用一批样本。小批量梯度是这批样本梯度的平均，因此每一步可能有波动。\n\n一个 epoch 表示遍历训练集一次。若有 1000 个样本，每批 128 个且保留末批，那么一个 epoch 有 8 次更新，最后一批有 104 个样本。\n\n训练时记录训练损失与验证指标。提前停止可以保留验证表现最好的参数；并不是训练轮数越多越好。",
    checks: [
      {
        question: "学习率是模型参数吗？",
        answer:
          "它是超参数，用于配置训练过程；权重和偏置才是这个模型中由梯度更新的参数。",
      },
    ],
  },
  {
    id: "softmax",
    title: "从分数到概率：Softmax",
    group: "二 · 学会训练",
    lead: "把模型、损失与梯度连接成一个完整分类器。这里最重要的结果是：对 logits 的交叉熵梯度等于预测概率减去标签。",
    source: "自学讲义 v1.1，第 6 章；第一至三周的 Softmax 回归部分。",
    body: "## 概率怎样从分数得到\n\n设 $\\mathbf z=W^\\top\\mathbf x+\\mathbf b$ 为各类别分数，Softmax 定义为：\n\n$$\np_c=\\frac{e^{z_c}}{\\sum_j e^{z_j}}.\n$$\n\n分数可以是任意实数，概率则为正且和为 1。取分数 $(1,2,3)$，得到概率约 $(0.0900,0.2447,0.6652)$。若第三类正确，损失约为 $0.4076$。\n\n## 稳定计算：先减去最大值\n\n给所有分数减同一个常数，不改变 Softmax。取 $m=\\max_jz_j$，最大指数就变为 1，可以避免大正数的指数溢出。\n\n$$\np_c=\\frac{e^{z_c-m}}{\\sum_j e^{z_j-m}},\\qquad\n\\ell=\\log\\sum_j e^{z_j-m}-(z_k-m).\n$$\n\n$k$ 是真实类别。后一式直接从分数得到交叉熵，避免正确类概率先下溢成零、再取对数。\n\n## 为什么梯度是 p − y\n\n标签满足 $\\sum_cy_c=1$ 时：\n\n$$\n\\ell=-\\sum_c y_c z_c+\\log\\sum_j e^{z_j},\n\\qquad\n\\frac{\\partial\\ell}{\\partial z_c}=p_c-y_c.\n$$\n\n这个式子已经把 Softmax 和交叉熵一起求导了，不要再额外乘一次 Softmax 导数。\n\n批量约定为 $X\\in\\mathbb R^{N\\times d}$、$W\\in\\mathbb R^{d\\times C}$，则\n\n$$\n\\nabla_WJ=\\frac1N X^\\top(P-Y)+\\lambda W,\\qquad\n\\nabla_{\\mathbf b}J=\\frac1N\\sum_i(P_i-Y_i).\n$$\n\n## 例题：正确类概率怎样升高\n\n一个样本 $\\mathbf x=(1,2)^\\top$，真实标签是第二类。权重与偏置初始化为零，因此三个概率都是 $1/3$。\n\n$$\n\\mathbf p-\\mathbf y=\n\\begin{pmatrix}1/3\\\\-2/3\\\\1/3\\end{pmatrix},\n\\qquad\n\\nabla_W\\ell=\n\\begin{pmatrix}\n1/3&-2/3&1/3\\\\\n2/3&-4/3&2/3\n\\end{pmatrix}.\n$$\n\n学习率为 $0.1$，同步更新权重与偏置后，分数变为 $(-0.2,0.4,-0.2)$。正确类概率升到约 $0.4767$，损失从 $1.0986$ 降到约 $0.7408$。\n\n## NumPy 中的核心计算\n\n~~~python\n# X: (N, d), W: (d, C), b: (C,), y: integer labels\nz = X @ W + b\nshift = z - z.max(axis=1, keepdims=True)\nexp_z = np.exp(shift)\np = exp_z / exp_z.sum(axis=1, keepdims=True)\n\nloss = np.mean(\n    np.log(exp_z.sum(axis=1))\n    - shift[np.arange(len(y)), y]\n)\nresidual = p.copy()\nresidual[np.arange(len(y)), y] -= 1\ndW = X.T @ residual / len(y) + lam * W\ndb = residual.mean(axis=0)\n~~~\n\n这里先算全部梯度，再执行参数更新；平均只做一次。",
    checks: [
      {
        question: "给每个 logit 都加 100，与都乘 100，结果一样吗？",
        answer:
          "同加常数不改变概率。乘以 100 会放大分数差，通常改变概率；存在唯一最大分数时，概率会更集中在该类别。",
      },
    ],
  },
  {
    id: "networks",
    title: "把线性模型接成神经网络",
    group: "三 · 理解网络",
    lead: "全连接层负责混合输入，激活函数引入非线性。理解这两个动作，就能逐层追踪一次前向传播。",
    source: "自学讲义 v1.1，第 7 章；第三周的神经元、激活与网络结构部分。",
    body: "## 一个神经元做两件事\n\n$$\nz=\\mathbf w^\\top\\mathbf x+b,\\qquad a=\\sigma(z).\n$$\n\n先做加权求和，再经过激活函数。若每层只有仿射变换，叠两层仍然是仿射变换：\n\n$$\nW_2(W_1\\mathbf x+\\mathbf b_1)+\\mathbf b_2\n=(W_2W_1)\\mathbf x+W_2\\mathbf b_1+\\mathbf b_2.\n$$\n\n所以增加层数本身并不会自动得到非线性表达能力。\n\n## 三种激活函数\n\n| 激活 | 定义 | 看梯度时要注意 |\n| --- | --- | --- |\n| Sigmoid | $1/(1+e^{-z})$ | 两端饱和；导数最大为 $1/4$ |\n| Tanh | $\\tanh z$ | 输出以零为中心；两端也会饱和 |\n| ReLU | $\\max(0,z)$ | 正半轴导数为 1；负半轴为 0 |\n\nReLU 在零点不可导，实现需采用约定值。正半轴导数为 1 有助于传播梯度，但不保证整个网络不会出现梯度消失或爆炸。\n\n## 逐层计算一次\n\n令\n\n$$\n\\mathbf x=\\begin{pmatrix}1\\\\2\\end{pmatrix},\\quad\nW_1=\\begin{pmatrix}1&-1\\\\0.5&1\\end{pmatrix},\\quad\n\\mathbf b_1=\\begin{pmatrix}0\\\\0.5\\end{pmatrix}.\n$$\n\n第一层净输入是 $\\mathbf z_1=(-1,3)^\\top$，经过 ReLU 后，$\\mathbf a_1=(0,3)^\\top$。\n\n第二层取 $W_2=\\begin{pmatrix}1&1\\\\-1&0.5\\end{pmatrix}$、$\\mathbf b_2=0$，得到 logits $(3,1.5)^\\top$，Softmax 概率约为 $(0.8176,0.1824)^\\top$。\n\n隐藏活性、logits、概率恰好都是二维向量，但含义不同。若真实标签为第二类，交叉熵约为 $1.7014$。\n\n## 参数量怎样数\n\n输入 4 维、隐藏层 5 个单元、输出 3 类，含偏置的参数量为：\n\n$$\n(5\\times4+5)+(3\\times5+3)=43.\n$$\n\n每一层的权重形状是“输出维数 × 输入维数”，每个输出单元再有一个偏置。\n\n## 表达能力与学得出来是两回事\n\n合适条件下，足够宽的单隐藏层网络可以逼近紧集上的连续函数。这是存在性结论：它不保证训练数据足够、不保证优化能找到那组参数，也不保证新数据上的表现。深度的价值在于可以复用和组合中间表示，但效果仍依赖具体问题。",
    checks: [
      {
        question:
          "输入 10 维、隐藏层 8 个单元、输出 4 类，含偏置时有多少参数？",
        answer: "$(10\\times8+8)+(8\\times4+4)=124$。",
      },
    ],
  },
  {
    id: "backprop",
    title: "反向传播：把误差传回来",
    group: "三 · 理解网络",
    lead: "反向传播计算梯度，优化器利用梯度更新参数。先算一个只有六个权重的网络，再看矩阵递推。",
    source: "自学讲义 v1.1，第 8 章；第三周第 53—67 页，包含重算与维度核对。",
    body: "## 先写清楚前向计算\n\n输入 $(x_1,x_2)=(1,0.5)$，目标 $y=4$。两个隐藏单元和输出为：\n\n$$\nh_1=w_1x_1+w_2x_2,\\quad\nh_2=w_3x_1+w_4x_2,\\quad\n\\hat y=w_5h_1+w_6h_2.\n$$\n\n初始权重是 $(0.5,1.5,2.3,3,1,1)$。于是 $h_1=1.25$、$h_2=3.8$、$\\hat y=5.05$，平方损失为 $E=\\frac12(5.05-4)^2=0.55125$。\n\n这个例子没有激活函数，是为了把链式法则的计算看清楚。\n\n## 从输出层向前计算\n\n输出敏感度 $\\partial E/\\partial\\hat y=1.05$。乘上局部输入，就得到输出层权重梯度：\n\n$$\n\\frac{\\partial E}{\\partial w_5}=1.05\\times1.25=1.3125,\\qquad\n\\frac{\\partial E}{\\partial w_6}=1.05\\times3.8=3.99.\n$$\n\n经过旧的 $w_5=w_6=1$ 传到隐藏层，再乘相应输入：\n\n| 参数 | 梯度 |\n| --- | --- |\n| $w_1$ | $1.05$ |\n| $w_2$ | $0.525$ |\n| $w_3$ | $1.05$ |\n| $w_4$ | $0.525$ |\n\n学习率为 $0.1$ 时，同步更新后得到\n\n$$\n\\mathbf w'=(0.395,1.4475,2.195,2.9475,0.86875,0.601).\n$$\n\n新预测约为 $3.1768$，新损失约为 $0.3388$。预测跨过真实值，损失仍可能下降。\n\n## 一般网络的误差递推\n\n对第 $l$ 层，定义 $\\boldsymbol\\delta^{(l)}=\\partial\\ell/\\partial\\mathbf z^{(l)}$。隐藏层逐元素激活时：\n\n$$\n\\boldsymbol\\delta^{(l)}\n=\\left((W^{(l+1)})^\\top\\boldsymbol\\delta^{(l+1)}\\right)\n\\odot\\sigma_l'(\\mathbf z^{(l)}).\n$$\n\n先经后一层权重累加各条路径的贡献，再乘本层激活导数。这里使用 $W^{(l+1)}$，而且 $\\odot$ 是逐元素乘法。\n\n局部参数梯度是外积：\n\n$$\n\\nabla_{W^{(l)}}\\ell\n=\\boldsymbol\\delta^{(l)}(\\mathbf a^{(l-1)})^\\top,\\qquad\n\\nabla_{\\mathbf b^{(l)}}\\ell=\\boldsymbol\\delta^{(l)}.\n$$\n\n外积恰好给出“本层宽度 × 上层宽度”的权重形状。批量训练对样本梯度求平均，再加入正则项。\n\n> 前向缓存 → 完整反向 → 同步更新。不要在反传途中改变权重，否则会混用不同参数点的梯度。",
    checks: [
      {
        question: "为什么隐藏层梯度不能用刚更新后的 w₆？",
        answer:
          "这一轮梯度应是损失在旧参数处的导数。提前更新会让不同梯度对应不同参数点，不再构成同一次完整反向传播。",
      },
    ],
  },
  {
    id: "autodiff",
    title: "自动微分与训练检查",
    group: "三 · 理解网络",
    lead: "自动微分把程序拆成有局部导数的小运算，再沿计算图组合导数。它不会替我们决定哪个损失才是正确目标。",
    source: "自学讲义 v1.1，第 9 章；第三周的计算图与自动微分部分。",
    body: "## 从一个复合函数开始\n\n考虑 $f(w,b)=1/(1+\\exp(-(wx+b)))$。把它拆成乘法、加法、取负、指数、加一、倒数，每个节点都只需知道自己的局部导数。\n\n在 $x=1,w=b=0$ 处，输出是 $0.5$。直接应用链式法则：\n\n$$\n\\frac{\\partial f}{\\partial w}\n=\\sigma(wx+b)(1-\\sigma(wx+b))x=0.25.\n$$\n\n如果沿计算图从输出倒推，倒数节点产生的 $-0.25$ 会在后续取负节点再乘 $-1$，最终也得到 $+0.25$。计算图里不能因为最后结果为正，就把中间负号略去。\n\n## 三种求导方法不要混淆\n\n| 方法 | 怎样做 | 适合注意的问题 |\n| --- | --- | --- |\n| 符号求导 | 变换代数表达式 | 表达式可能变得很大 |\n| 数值差分 | 扰动输入，再比较输出 | 有步长与浮点消减误差 |\n| 自动微分 | 组合实际执行运算的局部导数 | 仍有浮点误差；目标写错照样会求导 |\n\n中心差分是检查小模型梯度的办法：\n\n$$\n\\frac{\\partial J}{\\partial\\theta_j}\n\\approx\n\\frac{J(\\theta+\\varepsilon\\mathbf e_j)-J(\\theta-\\varepsilon\\mathbf e_j)}\n{2\\varepsilon}.\n$$\n\n$\\varepsilon$ 太大时近似误差明显，太小时浮点误差可能占主导。检查 ReLU 网络时，也要避免恰好跨过不可导点。\n\n## 为什么训练常用反向模式\n\n神经网络可能有许多参数，但最终损失通常是一个标量。反向模式从这个标量出发，复用中间结果，一次反向传播即可得到所有参数的梯度；代价是保存或重算中间量。\n\n逐参数差分则要反复计算函数，参数很多时成本很高。自动微分不是用“足够小的扰动”来近似梯度。\n\n## 遇到训练异常时检查什么\n\n1. 损失和任务是否一致，标签与 logits 是否传对。\n2. 所有梯度是否在同一轮参数上算出。\n3. 批量平均是否重复执行，梯度是否意外累积。\n4. 输入尺度、初始化和学习率是否让激活进入极端区域。\n5. 训练与验证的计算是否一致，是否存在数据泄漏。\n\n梯度消失或爆炸来自反向链条上的矩阵与激活导数反复相乘。某种激活函数能改善其中一部分问题，不等于能独自解决所有训练困难。",
    checks: [
      {
        question: "自动微分算出了梯度，就说明训练目标写对了吗？",
        answer:
          "不说明。它计算的是代码中实际定义的函数的导数。错误的损失也可能得到数值完全正确的梯度，所以需要独立的数学与任务核对。",
      },
    ],
  },
  {
    id: "review",
    title: "复习：能算出来才算过关",
    group: "四 · 自测",
    lead: "把前三周串成一个闭环：说清楚任务，检查形状，算损失与梯度，再判断模型是否泛化。先独立作答，再展开解答。",
    source:
      "自学讲义 v1.1，第 10 章；网页按知识主线重组自测，不包含作业提交包。",
    body: "## 画出一次完整训练\n\n用自己的话描述下面的循环，并指出每一步需要哪些量：\n\n$$\n(X,y)\\ \\longrightarrow\\ f_\\theta(X)\\\n\\longrightarrow\\ J(\\theta)\\\n\\longrightarrow\\ \\nabla_\\theta J\\\n\\longrightarrow\\ \\theta'.\n$$\n\n验证集不出现在参数梯度里，而是帮助选择配置与停止时刻。测试集则留作最终评价。\n\n## 三个层次的复习目标\n\n| 层次 | 可以怎样检查 |\n| --- | --- |\n| 能解释 | 不看笔记区分模型、损失与优化器 |\n| 能手算 | 完成 Softmax、一次更新和六权重反传 |\n| 能检查实现 | 找到转置错误、漏平均和更新顺序问题 |\n\n一开始不必追求背下所有公式。先保证每个符号能说清楚、每个矩阵乘法的形状能对上，再把计算连接起来。下面的问题覆盖最容易混淆的地方。",
    checks: [
      {
        question: "模型、损失和优化算法分别负责什么？",
        answer:
          "模型决定如何从输入得到预测，例如 $\\hat y=\\mathbf w^\\top\\mathbf x+b$；损失衡量预测结果，例如平方损失；优化算法决定怎样调整参数，例如梯度下降。",
      },
      {
        question: "真实类别为第三类，概率为 (0.2, 0.3, 0.5)，交叉熵是多少？",
        answer: "$-\\log0.5\\approx0.6931$。只取正确类别对应的负对数。",
      },
      {
        question: "1000 个样本、每批 128 个且保留末批，一个 epoch 有几次更新？",
        answer: "8 次；前 7 批各 128 个，最后一批 104 个。",
      },
      {
        question: "没有非线性激活的三层网络，能仅靠叠层表达 XOR 吗？",
        answer:
          "不能。仿射变换的组合仍是仿射变换，不能产生 XOR 所需的非线性边界。",
      },
      {
        question: "反向传播与梯度下降有什么区别？",
        answer:
          "反向传播负责计算梯度，梯度下降负责用梯度更新参数。计算出梯度之后，可以选择不同优化算法。",
      },
      {
        question: "Sigmoid 在 0 处的值与导数分别是多少？",
        answer: "$\\sigma(0)=0.5$，$\\sigma'(0)=0.5(1-0.5)=0.25$。",
      },
      {
        question: "训练误差一直下降，可以断言模型越来越好吗？",
        answer:
          "不能。还要看独立验证数据上的表现与任务指标。拟合训练数据的能力和对新数据的泛化能力不是同一个量。",
      },
      {
        question: "通用逼近定理保证梯度下降会找到好参数吗？",
        answer:
          "不保证。它在相应条件下说明某组参数存在，不直接保证优化过程、有限样本学习或泛化表现。",
      },
    ],
  },
] satisfies CourseChapter[];
